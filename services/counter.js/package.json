{
  "name": "counter",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "node ./src/main.js",
    "start:dev": "npx nodemon",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "npx eslint . --ext .js",
    "lint-and-fix": "npx eslint . --ext .js --fix",
    "prettier-format": "npx prettier --config .prettierrc 'src/**/*.js' --write",
    "prettier-watch": "npx onchange 'src/**/*.js' -- prettier --write {{changed}}"
  },
  "dependencies": {
    "flatend": "0.0.7"
  },
  "devDependencies": {
    "eslint": "^7.3.1",
    "eslint-config-prettier": "^6.11.0",
    "eslint-plugin-prettier": "^3.1.4",
    "nodemon": "^2.0.4",
    "onchange": "^7.0.2",
    "prettier": "^2.0.5"
  }
}